# modules group functions & variables

# .py files are modules
# - they have their own namespace

# the interpreter searches for modules in this order:
# 1. build-in modules
# 2. sys.path

# list the names a module defines with dir(module_name), or currently defined names with dir()